<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shiguang.mapper.CreatorMapper">

    <!-- 统计当前用户累计浏览量：只统计已发布/下架作品，草稿与已删除不计入 -->
    <select id="countViews" resultType="long">
        SELECT COUNT(*)
        FROM work_views v
                 JOIN works w ON w.id = v.work_id
        WHERE w.author_id = #{authorId}
          AND w.status IN ('published', 'offline')
    </select>

    <!-- 统计今日新增浏览量：起始时间由服务端以本地时区计算传入，避免 DB 时区漂移 -->
    <select id="countTodayViews" resultType="long">
        SELECT COUNT(*)
        FROM work_views v
                 JOIN works w ON w.id = v.work_id
        WHERE w.author_id = #{authorId}
          AND w.status IN ('published', 'offline')
          AND v.created_at &gt;= #{startOfToday}
    </select>

    <!-- 统计当前用户累计点赞数：只统计已发布/下架作品 -->
    <select id="countLikes" resultType="long">
        SELECT COUNT(*)
        FROM work_likes wl
                 JOIN works w ON w.id = wl.work_id
        WHERE w.author_id = #{authorId}
          AND w.status IN ('published', 'offline')
    </select>

</mapper>
